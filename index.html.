<!DOCTYPE html>
<html lang="ar">
<head>
<meta charset="UTF-8">
<title>Ali ❤️ Mariam</title>

<style>
body{
    margin:0;
    font-family:'Segoe UI',sans-serif;
    background:radial-gradient(circle at top,#1a1a2e,#16213e,#0f3460);
    color:white;
    text-align:center;
    overflow:hidden;
}

/* نجوم */
.star{
    position:fixed;
    width:2px;
    height:2px;
    background:white;
    opacity:0.8;
    animation: twinkle 2s infinite alternate;
}

@keyframes twinkle{
    from{opacity:0.2;}
    to{opacity:1;}
}

/* شاشة القفل */
#lock{
    position:fixed;
    width:100%;
    height:100%;
    background:#0f3460;
    display:flex;
    flex-direction:column;
    justify-content:center;
    align-items:center;
}

input{
    padding:12px;
    border-radius:30px;
    border:none;
    text-align:center;
    margin:10px;
}

button{
    padding:10px 25px;
    border:none;
    border-radius:30px;
    background:#e94560;
    color:white;
    font-weight:bold;
    cursor:pointer;
}

.container{
    display:none;
    padding-top:80px;
    animation:fadeIn 2s ease forwards;
}

@keyframes fadeIn{
    from{opacity:0;}
    to{opacity:1;}
}

.card{
    background:rgba(255,255,255,0.1);
    margin:20px auto;
    padding:25px;
    border-radius:20px;
    width:85%;
    backdrop-filter:blur(10px);
}

/* تايبينج */
.typing{
    border-right:3px solid white;
    white-space:nowrap;
    overflow:hidden;
    display:inline-block;
    animation:typing 3s steps(20), blink .7s infinite;
}

@keyframes typing{
    from{width:0;}
    to{width:200px;}
}

@keyframes blink{
    50%{border-color:transparent;}
}

</style>
</head>

<body>

<div id="lock">
    <h2>🔐 Enter Password</h2>
    <input type="password" id="pass" placeholder="LOVE">
    <button onclick="openSite()">Open</button>
    <p id="error" style="color:red;"></p>
</div>

<div class="container" id="site">

<h1>❤️ علي & <span class="typing">مريم</span> ❤️</h1>

<div class="card">
<h2>📅 بداية حبنا</h2>
<p>1 / 6 / 2025</p>
<p id="days"></p>
</div>

<div class="card">
<h2>⏳ العد التنازلي لأول سنة</h2>
<p id="countdown"></p>
</div>

<div class="card">
<h2>💌 رسالة حب</h2>
<p>
يا مريم...  
انتي مش بس حبيبتي، انتي اختياري كل يوم ❤️  
وجودك في حياتي هو أجمل حاجة حصلتلي،  
وهفضل أحبك وأختارك مهما حصل.
</p>
</div>

</div>

<!-- موسيقى -->
<audio id="music" loop>
<source src="music.mp3" type="audio/mpeg">
</audio>

<script>

/* كلمة السر */
function openSite(){
    if(document.getElementById("pass").value==="LOVE"){
        document.getElementById("lock").style.display="none";
        document.getElementById("site").style.display="block";
        document.getElementById("music").play();
        createStars();
    }else{
        document.getElementById("error").innerText="Wrong Password ❤️";
    }
}

/* نجوم */
function createStars(){
    for(let i=0;i<100;i++){
        let star=document.createElement("div");
        star.className="star";
        star.style.top=Math.random()*100+"vh";
        star.style.left=Math.random()*100+"vw";
        document.body.appendChild(star);
    }
}

/* عداد الأيام */
let start=new Date("2025-06-01");
let now=new Date();
let diff=now-start;
let days=Math.floor(diff/(1000*60*60*24));
document.getElementById("days").innerHTML=
"عدى على حبنا "+days+" يوم ❤️";

/* العد التنازلي */
let eventDate=new Date("2026-06-01");
setInterval(()=>{
    let now=new Date();
    let distance=eventDate-now;

    let d=Math.floor(distance/(1000*60*60*24));
    let h=Math.floor((distance%(1000*60*60*24))/(1000*60*60));
    let m=Math.floor((distance%(1000*60*60))/(1000*60));
    let s=Math.floor((distance%(1000*60))/1000);

    document.getElementById("countdown").innerHTML=
    d+" يوم "+h+" ساعة "+m+" دقيقة "+s+" ثانية ❤️";

    if(distance<0){
        document.getElementById("countdown").innerHTML=
        "🎉 انهارده عيد حبنا الأول ❤️";
    }
},1000);

</script>

</body>
</html>
